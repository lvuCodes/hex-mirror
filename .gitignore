yarn.lock
.DS_Store
pnpm-lock.yaml

# Vendored Obsidian plugin lockfiles — keep only the app's own package-lock.json
.obsidian/**/package-lock.json
*.log
node_modules
dist
*.tsbuildinfo

# Tooling output / caches
coverage
.vite
vite.config.ts.timestamp-*
.eslintcache
*.local
